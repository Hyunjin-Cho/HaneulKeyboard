# ROADMAP

기능 로드맵 + 보류 중인 설계 메모. 진행 상황은 GitHub issues 참조.

## 영타 자동 변환 v2 — 룰 기반 판정 (2026-06-06 구현)

사용자 조건식 11개 기반으로 점수 대신 결정적 룰로 구현. **핵심 원칙(적대적 리뷰 2회로 검증): 멀쩡히 조합된 한글은 구조만으론 진짜 한국어와 구분 불가** (며새=auto와 모든=ahems가 코드엔 동일). 따라서 clean Hangul은 좁은 채널로만 변환:

- **MAIN** (깨진 자모): standalone jamo + 모음키 + **broad 사전(web2)** 일치. 깨졌다는 사실 자체가 한국어 아님을 증명하므로 전체 사전 안전. 메ㅔㅣㄷ→apple, ㅡㅐㄱ무→moran(조건10).
- **R1 모음 시작** (조건 1): 단독 모음 시작 = 한국어 불가 → ㅑ→i. SHORT_WORDS(슬랭 제외) 또는 사전.
- **R3 음절 실존성** (조건 3): KS X 1001 완성형 미포함 음절(EUC-KR 인코딩 불가 = 솓) → 영어 후보. 데이터 파일 없이 구현.
- **R5 긴 clean 단어** (조건 8·11): ≥6키 + **curated 사전(supplement만, web2 제외)** → 퍄녀미→visual, 두샤시드둣→entitlement. web2를 쓰면 야구인=dirndls·힘찬=glacks 같은 obscure 충돌이 진짜 한국어를 파괴하므로 curated 한정 + exact match.
- **R2 영어 문맥** (조건 2·5): 직전 commit이 영어 + **SHORT_WORDS 전용** → 새→to, ㅁ→a. clean 한글을 문맥으로 건드리는 유일한 룰이라 화이트리스트만 (web2 확장은 좀/책/형/곧 파괴 → 리뷰서 critical, 되돌림). 문맥은 스페이스/쉼표로만 유지; 마침표·엔터·클릭·포커스이동·**커밋텍스트 백스페이스** 시 리셋.
- 조건 6·7(자음/모음 연속)은 MAIN의 "깨진 자모" 신호가 동일 정보 — 받침 ㄺ/ㄼ/ㅀ과 복모음 와/왜/의는 조합 성공으로 자동 보호.
- 조건 4·9는 사전 보충 (readme/xcode/kpop/opus 등).
- ㅢ(ml)/ㅐㅏ(ok)는 흔한 슬랭이라 context-only — 영어 문맥서만 변환.
- **조건 10(모음 반복)은 별도 룰 없이 MAIN이 처리**: 떠 있는 모음도 깨진 자모라
  MAIN의 (모음+사전) 조건에 걸림. mini=ㅡㅑㅜㅑ(supplement 등재→변환), kpop=ㅏㅔㅐㅔ.
  영어로 말이 안 되는 모음 나열(ㅗㅜㅑ→hni, ㅜㅡㅜ→nmn 이모티콘)은 사전에 없어
  자동으로 한글 유지. "사전 없이 모음 3개 무조건 변환"(초기 R6)은 이 슬랭/이모티콘을
  깨서 폐기 — 사용자 ㅗㅜㅑ 반례로 "영어로 말 될 때만" 정제 (Opus 검증, 한국어 373단어
  false positive 0). 규칙 전체 표는 [`RULES.md`](./RULES.md).

**제거된 룰 — R4 (사전 없는 고유명사 감지)**: ≥3 distinct 자모 + 음절 게이트가 한국어 채팅 슬랭(ㅇㄱㄹㅇ임, 아ㅋㅋㄹㅇ)을 ASCII로 파괴 (리뷰 critical). 깨진 자모 고유명사(moran)는 MAIN이 잡고, 그 외 고유명사는 supplement에 추가하는 방향으로 대체. "완전 사전 없는 감지"는 슬랭 파괴 때문에 폐기.

### 남은 한계 / 후보 신호

- **문장 첫 단어가 짧고 멀쩡히 조합되는 경우** (auto→며새): 커밋 시점에 뒤 단어를 모르고, 이미 커밋된 텍스트의 소급 수정은 의도적으로 배제(HanglishIME의 이벤트 주입 함정). clean 한글이라 R5(≥6키)도 미달, R2는 문맥 없음 → 유지됨. 정상 한국어 보호와 맞바꾼 한계.
- **흔한 영어 단어 목록(R5) 확장**: 현재 R5는 supplement.txt 한정이라 사용자가 예시로 든 단어 위주. important/beautiful 같은 일반 긴 영어는 미커버. 빈도순 영어 단어 수천 개를 supplement에 점진 추가 필요 (단, 각 추가어가 clean Hangul 조합으로 진짜 한국어와 충돌하지 않는지 감사 필요 — 야구인=dirndls류).
- **clean Hangul 영어 ↔ 진짜 한국어 구분의 근본 한계**: 빈도 신호 없이는 며새(auto, 희귀 한국어)와 모든(common 한국어)을 못 가름. v3: 예측 입력이 쌓은 사용자 한국어 빈도로 "이 사용자가 쓴 적 없는 한글 = 영어 후보" veto 추가하면 R5/R2를 안전하게 넓힐 수 있음.

- **visual→퍄녀미**: 퍄가 KS X 1001에 포함돼 있으면 구조 신호가 없음 (사전 보충으로도 깨진 자모가 없어 MAIN 룰 미발동) — 한국어 단어 사전 도입 시 해결 (word-level 실존성).
- **손 교대 리듬** (사용자 제안 2026-06-06): 두벌식은 왼손=자음/오른손=모음이라 한국어는 좌우 교대가 규칙적. 교대 패턴 점수는 현재 "깨진 자모" 신호와 정보가 겹치지만, 멀쩡히 조합되는 영단어(visual류)를 잡는 연속 점수로 발전 가능 — n-gram 없는 경량 신호 후보.
- 짧은 한글-영어 충돌 단어는 R2(문맥)로만: 해→go, 내→so — "okay 해"류 오변환 리스크는 화이트리스트 조정으로 제어.

## (구) 점수 기반 설계 메모 — 참고용 보존

### 문제

v1은 "한글로서 깨진 단어"만 변환한다 (의도적 보수 설계 — 오변환 0 우선).
그래서 **멀쩡한 한글 음절로 조합되는 영단어**는 못 잡는다:

| 입력 | 조합 결과 | 기대 |
|---|---|---|
| `the` | 솓 | the |
| `to` | 새 | to |
| `i` | ㅑ | I |
| `su` | 너 | su |

### 접근 (점수 기반)

깨진 자모 신호 대신 **양쪽 언어 그럴듯함을 점수로 비교**:

- **한국어 희귀도**: 조합 결과가 실제 한국어에서 쓰이는 음절/어절인가.
  "솓"은 표준어에 거의 없는 음절 → 한국어 점수 낮음.
  (음절 빈도표는 공개 말뭉치 통계로 생성 — 라이선스 확인 필수)
- **영어 빈도**: 키 시퀀스가 영어 고빈도 단어인가. "the"는 top-5 → 영어 점수 최고.
- **변환 조건**: 영어 점수가 한국어 점수를 **큰 마진**으로 이길 때만.
  마진은 보수적으로 — 진짜 한국어를 건드리는 순간 신뢰 끝.

### 안전장치

- 1–2키 초단어(i, to, am...)는 점수 무관하게 **수동 검증 화이트리스트**로만 처리
  (ㅑ/새/메 등이 단독으로 쓰이는 한국어 맥락 존재 — 사례별 검토 필요).
- 한국어 음절 빈도표 대조로 "흔한 한글인데 영단어로도 해석되는" 충돌 목록을
  먼저 전수 생성 → 오변환 0 확인 후 출시 (v1 리뷰에서 쓴 방법 재사용).
- v1과 동일: active 경계(직접 친 스페이스/문장부호)에서만 발동.

### 의존성

- 예측 입력의 단어 빈도 데이터가 쌓이면 "사용자 본인의 한국어" 기준으로
  희귀도를 보정할 수 있음 → 예측 입력 이후 진행이 자연스러움.

## 예측 입력 (자동완성) — 진행 중

이슈 #2. 한국어 전용 (영타 자동 변환과 독립 — 예측은 한글 단어만 학습/제안).
조건식 사용자 협의 중. 렌더링은 floating panel 패턴 (조사 완료, WORKLOG 2026-06-05 참조).
