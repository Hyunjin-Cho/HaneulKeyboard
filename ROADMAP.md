# ROADMAP

기능 로드맵 + 보류 중인 설계 메모. 진행 상황은 GitHub issues 참조.

## 영타 자동 변환 v2 — 점수 기반 판정 (보류, 예측 입력 다음)

### 문제

v1은 "한글로서 깨진 단어"만 변환한다 (의도적 보수 설계 — 오변환 0 우선).
그래서 **멀쩡한 한글 음절로 조합되는 영단어**는 못 잡는다:

| 입력 | 조합 결과 | 기대 |
|---|---|---|
| `the` | 솓 | the |
| `to` | 새 | to |
| `i` | ㅑ | I |
| `su` | 너 | su |

### 접근 (점수 기반)

깨진 자모 신호 대신 **양쪽 언어 그럴듯함을 점수로 비교**:

- **한국어 희귀도**: 조합 결과가 실제 한국어에서 쓰이는 음절/어절인가.
  "솓"은 표준어에 거의 없는 음절 → 한국어 점수 낮음.
  (음절 빈도표는 공개 말뭉치 통계로 생성 — 라이선스 확인 필수)
- **영어 빈도**: 키 시퀀스가 영어 고빈도 단어인가. "the"는 top-5 → 영어 점수 최고.
- **변환 조건**: 영어 점수가 한국어 점수를 **큰 마진**으로 이길 때만.
  마진은 보수적으로 — 진짜 한국어를 건드리는 순간 신뢰 끝.

### 안전장치

- 1–2키 초단어(i, to, am...)는 점수 무관하게 **수동 검증 화이트리스트**로만 처리
  (ㅑ/새/메 등이 단독으로 쓰이는 한국어 맥락 존재 — 사례별 검토 필요).
- 한국어 음절 빈도표 대조로 "흔한 한글인데 영단어로도 해석되는" 충돌 목록을
  먼저 전수 생성 → 오변환 0 확인 후 출시 (v1 리뷰에서 쓴 방법 재사용).
- v1과 동일: active 경계(직접 친 스페이스/문장부호)에서만 발동.

### 의존성

- 예측 입력의 단어 빈도 데이터가 쌓이면 "사용자 본인의 한국어" 기준으로
  희귀도를 보정할 수 있음 → 예측 입력 이후 진행이 자연스러움.

## 예측 입력 (자동완성) — 진행 중

이슈 #2. 한국어 전용 (영타 자동 변환과 독립 — 예측은 한글 단어만 학습/제안).
조건식 사용자 협의 중. 렌더링은 floating panel 패턴 (조사 완료, WORKLOG 2026-06-05 참조).
